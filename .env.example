# ==============================================================================
# Moodle Docker - Variáveis de Ambiente
# Copie este arquivo para .env e configure os valores
# ==============================================================================

# ------------------------------------------------------------------------------
# MOODLE
# ------------------------------------------------------------------------------
# URL de acesso ao Moodle (com https://)
MOODLE_WWWROOT=https://moodle.example.com

# Salt para hashes de senha (gere com: openssl rand -hex 32)
MOODLE_PASSWORD_SALT=

# Chave de upgrade (opcional, para upgrades via CLI)
MOODLE_UPGRADE_KEY=

# ------------------------------------------------------------------------------
# BANCO DE DADOS (MySQL/MariaDB)
# ------------------------------------------------------------------------------
  MOODLE_DB_HOST=<external-mysql-ip>
  MOODLE_DB_PORT=3306

# Nome do banco de dados
MOODLE_DB_NAME=moodle

# Usuário do banco de dados
MOODLE_DB_USER=moodle

# Senha do banco de dados (use senha forte!)
# Gere com: openssl rand -base64 32
MOODLE_DB_PASSWORD=

# Senha root do MySQL (apenas para desenvolvimento)
MYSQL_ROOT_PASSWORD=

# ------------------------------------------------------------------------------
# REDIS (Cache de Sessão)
# ------------------------------------------------------------------------------
# Senha do Redis (opcional, mas recomendado)
# Gere com: openssl rand -base64 32
REDIS_PASSWORD=

# ------------------------------------------------------------------------------
# DEBUG (APENAS DESENVOLVIMENTO!)
# ------------------------------------------------------------------------------
# Habilita debug do Moodle (NUNCA em produção!)
# MOODLE_DEBUG=true

# ==============================================================================
# PROXY DE BUILD (rede CDHU com inspeção SSL FortiGate)
# ==============================================================================
# Usado apenas durante `docker compose build`. Deixe VAZIO fora da rede CDHU.
# As CAs correspondentes ficam em config/ca/ (gere com make setup-proxy).
BUILD_PROXY=http://10.71.48.17:8080
BUILD_NO_PROXY=localhost,127.0.0.1,::1,.cdhu.sp.gov.br,.sp.gov.br
